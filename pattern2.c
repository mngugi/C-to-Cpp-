#include <stdio.h>

int main (){
int k, n ;

    for (k = 6 ; k >= 1; k--) { // controls the number of rows 6 to 1
        for (n = 1; n <= k; n++){
         // controls the number of +
            printf("+");
            }
            printf("\n");


    }
        return 0 ;
 }
